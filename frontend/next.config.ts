import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    allowedDevOrigins: ['specifically-bristol-timely-screensavers.trycloudflare.com'],
};

export default nextConfig;
